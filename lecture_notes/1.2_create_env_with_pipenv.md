Following command will create python3 virtual environment.
```
$ pipenv --three
```

You can activate the environment like
```
$ pipenv shell
```

You can install django like
```
$ pipenv install Django==2.2.5
```
